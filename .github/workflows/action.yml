name: run-blazemeter-test-actions-workflow
env:
  apikey: "673e0985b554e480881605e1"
  apisecret: "c9dbf4089ebff1d4d484367b8061a2312b29902cfb3ca928914c90853aac0de7c9249d26"
  testid: "9997493"
  workspaceid: "872110"
  datamodelid: "a820b392-eb9f-60bf-49c9-7514bdc994c0"
  modeldata: "{'email': 'test123@mailinator.com', 'password': '123456123'}"
on: push
jobs:
  run-existing-test:
    runs-on: ubuntu-latest
    steps:    
      - name: Run Blazemeter test
        #uses: ./ # Uses an action in the root directory
        uses:  dnyanehblazerunner/BlazeAction@v2
        id: run-test
        with:
          apikey: ${{env.apikey}}
          apisecret: ${{env.apisecret}}
          testid: ${{env.testid}}
          workspaceid: ${{env.workspaceid}}
          datamodelid: ${{env.datamodelid}}
          modeldata: ${{ toJSON(env.modeldata) }}
          
